[Unit]
Description=Boot partition
DefaultDependencies=no
Conflicts=umount.target
Before=local-fs.target umount.target rauc-mark-good.service rauc.service
After=swap.target

[Mount]
What=/dev/sda1
Where=/boot
Type=auto

[Install]
WantedBy=multi-user.target
